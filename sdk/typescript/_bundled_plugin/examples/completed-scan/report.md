# 安全审查：example/repo

## 审查范围

本次扫描审查了以下列出的规范包含路径和排除项。

- 扫描模式：仓库
- 目标类型：git_worktree
- 目标 ID：target_sha256_example
- 修订版本：deadbeef
- 快照摘要：codex-security-snapshot/v1:sha256:ed88f96a4c1a06603a41b3f261f59c3de2555c367ef6ad3bb8b9e483495d34eb
- 清单策略：仓库
- 包含路径：.
- 排除路径：无
- 运行时或测试状态：未记录

### 扫描摘要

| 字段 | 值 |
| --- | --- |
| 可报告发现 | 1 |
| 严重程度分布 | 高：1 |
| 置信度分布 | 高：1 |
| 覆盖率 | 完整 |
| 验证模式 | 未记录 |

规范产物：`scan-manifest.json`、`findings.json` 和 `coverage.json`。本报告是这些文件的确定性投影。

## 威胁模型

未记录明确的规范威胁模型摘要。

## 发现

| 发现 | 严重程度 | 置信度 | 详细说明 |
| --- | --- | --- | --- |
| [不安全的归档解压可能逃逸输出目录](#finding-1) | 高 | 高 | 内联如下 |

### 置信度等级

| 标签 | 含义 |
| --- | --- |
| 高 | 直接证据支持该发现，且不存在实质性未解决的阻碍因素。 |
| 中 | 证据支持存在合理问题，但仍需实质性运行时或可达性证明。 |
| 低 | 证据不完整，仅保留该项目以供明确跟进。 |

<a id="finding-1"></a>

### [1] 不安全的归档解压可能逃逸输出目录

| 字段 | 值 |
| --- | --- |
| 严重程度 | 高 |
| 置信度 | 高 |
| 置信度依据 | 直接源码追踪到达文件系统写入，且无包含性检查。 |
| 类别 | 路径遍历 |
| CWE | CWE-22 |
| 受影响行 | src/extract.py:41-44 |

#### 摘要

攻击者控制的路径在未经过包含性验证的情况下到达文件系统写入操作。

#### 验证

直接源码追踪到达文件系统写入，且无包含性检查。验证细节未单独记录。

#### 数据流

规范发现记录了 src/extract.py:41-44 处的受影响路径，但未记录扩展的源到汇叙述。

#### 可达性

除规范发现摘要和受影响位置外，未记录可达性信息。

#### 严重程度

**高** — 扫描分配了高严重程度；未记录单独的规范严重程度依据。

额外的运行时或部署证据可能提高或降低此严重程度。

#### 修复建议

规范化目标路径，并拒绝逃逸解压根目录的条目。

测试：
- 断言解压名为 `../escape.txt` 的归档条目会失败，且不会在解压根目录之外写入文件。

预防性控制：
- 将所有归档解压操作路由至一个辅助函数，该函数负责规范化并验证目标路径。

## 已审查面

| 面 | 风险领域 | 结果 | 备注 |
| --- | --- | --- | --- |
| 归档解压 | 未记录 | 已报告 | 未记录额外的规范备注。 |