# 安全政策

Codex Security 是一款本地工具，用于审查您信任且有权评估的仓库。本政策说明了哪些安全问题属于处理范围，以及如何报告这些问题。

## 报告 Codex Security 中的漏洞

请通过 [OpenAI 的 Bugcrowd 项目](https://bugcrowd.com/engagements/openai) 私下报告 CLI、SDK、捆绑插件、扫描运行时或已发布构建产物中的漏洞。

请勿在 GitHub Issue 或 Pull Request 中发布未修复的漏洞、漏洞利用代码、凭据、敏感扫描结果或概念验证。普通 Bug、文档和功能请求请使用公开 Issue。

OpenAI 的[漏洞协同披露政策](https://openai.com/policies/coordinated-vulnerability-disclosure-policy/)说明了报告流程、保密要求和项目资格。

## 适用范围和支持版本

本政策适用于：

- 已发布的 `@openai/codex-security` 包和 `codex-security` CLI。
- TypeScript SDK，包括目标选择、身份验证、配置、执行和结果验证。
- 官方版本中捆绑的 Codex Security 插件、解释器和 Codex 运行时。
- 扫描输出，包括清单、发现结果、覆盖率、报告、SARIF 和扫描历史。
- 官方包、构建和发布版本的完整性。

请确认问题影响的是最新发布的包或当前默认分支。请提供包版本，并在相关时提供提交哈希、插件版本和操作系统。如果您在旧版本中发现问题，请说明受支持的版本是否也受到影响。

## 威胁模型

Codex Security 在您的本地操作系统账户下运行。请仅扫描您信任且拥有或获得明确评估权限的仓库。拥有评估仓库的权限并不意味着您可以信任该仓库。

您选择的仓库、您的 Git 安装以及您选择的工具和配置均以您现有的本地权限运行。正常的 Git 操作可以使用仓库配置、钩子、过滤器、属性、凭据助手、工作树以及 `PATH` 中的可执行文件。这些不构成独立的安全边界。

### 本地信任和攻击者前提条件

该产品不隔离共享同一操作系统账户、凭据或本地状态的用户、任务、仓库或扫描作业。请勿将共享的本地状态视为多用户或多租户系统。

私有 Codex 状态、工作台数据库、输出目录和恢复回执与您的操作系统账户之间不构成独立的安全边界。如果报告依赖于更改这些内容，则必须说明攻击者控制的输入如何通过受支持的工作流程到达这些内容，并跨越安全边界。

信任一个仓库并不授权无关操作。仓库内容、文件名、符号链接、模型输出、补丁、服务响应和导入的构建产物均属于数据。它们不授权另一个目标、更广泛的范围、不同的凭据、无关的读取或写入、未经批准的补丁或网络目的地、被绕过的限制，或对不完整覆盖率给出通过的结果。

### 扫描的运行方式

每次扫描都使用产品的 `codex_security_scan` 文件系统配置文件和自动审批审查。其基线配置文件允许读取本地文件系统，并允许写入工作区根目录和选定的扫描状态目录。请求会自动审查，无需交互式提示；已批准的请求可以为特定操作授予额外权限。设置 `approval_policy="never"` 可拒绝所有请求。

通过 `--codex` 或 SDK `codexOverrides` 设置 `approval_policy`、`approvals_reviewer`、`sandbox_mode` 或权限，不能替代自动审查器或基线文件系统配置文件。严格的 `approval_policy="never"` 覆盖（包括所选配置文件中的设置）会被保留。保存的扫描会保留其有效的审批策略，旧扫描在重新运行时仍保持全部拒绝。独立执行的主机和网络限制仍然适用。

扫描和工作台子进程可以继承您的环境。工作台会移除 `OPENAI_API_KEY` 和 `CODEX_API_KEY`，但不会移除所有凭据。其他变量（如 `GITHUB_TOKEN` 或 `AWS_SECRET_ACCESS_KEY`）可能仍可供本地子进程使用。请仅使用扫描所需的环境凭据来运行扫描。

### 安全边界

安全问题必须跨越产品实际提供的边界：

- 仅扫描选定的目标和请求的范围；仅写入授权的输出路径。
- 尊重明确选择的凭据和文档化的成本控制。
- 确保凭据、私有源代码和扫描结果不会进入操作员未授权的模型请求、日志、报告和网络目的地。
- 应用扫描的实际文件系统和执行配置文件，并尊重独立于扫描执行的主机或网络限制。
- 不通过符号链接或被替换的文件进行未授权的读取或写入。
- 仅当扫描结果与审查的范围、文档化的模式和声明的排除项一致时，才将扫描标记为完成。
- 保护官方包、捆绑运行时、依赖项、构建产物和发布凭据免受未经授权的更改。

## 范围内报告

请报告官方版本中可重现的问题，例如：

- 未经授权将凭据、私有源代码或扫描结果发送给其他安全主体、模型请求或网络目的地。
- 模型或远程输入绕过扫描的有效权限或独立执行的主机、执行、文件系统或网络限制。
- 超出您授权操作的扫描、补丁、文件写入或网络请求。
- 忽略您明确选择的目标、路径范围、凭据或文档化的成本限制。
- 路径遍历、符号链接、归档或文件替换竞争，导致写入到批准的输出之外，或将无关的本地文件发送给模型。
- 不完整、伪造或范围错误的扫描被接受为完成或作为通过的 CI 结果。
- GitHub、包、更新、依赖或模型服务的输入导致未经授权的本地操作或破坏发布版本。
- 已发布包、捆绑运行时、构建或发布流程中可触达的漏洞。
- 资源耗尽达到受支持的服务、CI 进程或其他实际可用性边界。

## 通常不在范围内

以下情况本身不构成安全漏洞：

- 在您授予的权限范围内读取选定的仓库文件、解析工作树、运行 Git，或使用配置的钩子、过滤器、凭据助手和可执行文件。
- 假设已控制您的操作系统账户、受信任的本地 Git 设置、私有 Codex 状态或私有扫描输出的攻击，但未说明受支持的输入如何获得该控制权。
- 依赖于您故意授予插件、解释器或可执行文件您的账户已有的权限的声明。
- 已共享您的账户和本地状态的进程对私有扫描状态、工作台数据库、结果、恢复回执或扫描历史进行访问、取消或更改。
- 提示注入、意外模型输出、遗漏的发现、误报或未签名的审查回执，未跨越边界或导致受支持的安全门接受不完整或失实的覆盖率。
- 文档化的排除项、忽略规则、估算或 Git 行为在扫描及其结果中得到准确反映。
- 在明确请求的限制仍按文档执行时，披露的估算不确定性或不可避免的进行中成本超支。
- 终端格式或控制字符，但在受支持的终端中没有证明的安全影响。
- 假设受信任的发布运行器、镜像仓库或依赖供应商被攻破，但没有从不可信输入到受保护发布版本的路径，或绕过其完整性控制。
- 依赖公告、理论攻击或旧包版本，但对受支持的发布版本没有可重现的影响。
- 处理您故意选择的文件、文档或仓库速度缓慢。
- 正在被扫描的第三方仓库中的漏洞。
- 已发布的运行时或发布流程无法触达的文档、测试、测试夹具或开发代码。

托管服务、多用户安装、Pull Request CI 和导入的第三方构建产物可能具有不同的信任边界。对于这些情况，请指明部署方式、攻击者控制的输入、受影响的组件和实际边界。存储多个本地扫描并不会使 CLI 成为多租户系统。

如果您不确定某个发现是否在范围内，请私下报告。

## 报告中应包含的内容

请包含：

- 受影响的组件、包版本、插件版本和提交哈希。
- 您的平台、身份验证方法、扫描模式和目标类型。
- 攻击者的初始权限和跨越的安全边界。
- 在受支持的版本或默认分支中重现问题的最简步骤。
- 预期和实际行为、影响以及任何已知的缓解措施。
- 重现问题所需的脱敏日志或扫描产物。

请移除 API 密钥、访问令牌、客户数据和私有源代码，除非私有报告需要这些信息且您有权共享。切勿在概念验证中包含实时凭据。

## 报告被扫描仓库中的发现

如果扫描在他人仓库中发现漏洞，请遵循该项目的安全政策，并仅与授权人员共享该发现。OpenAI 的 Bugcrowd 项目涵盖 OpenAI 产品和服务，不涵盖其他项目中的漏洞。

## 安全运行扫描

- 仅扫描您信任且拥有或获得明确评估权限的仓库。
- 在应用或合并补丁之前，请查看仓库说明并检查补丁。
- 仅传递扫描所需的凭据。本地子进程可能继承其他环境变量。
- 将您的凭据和 Codex 主目录放在仓库之外。
- 将扫描状态、发现结果、报告、日志和 SARIF 存储在 Git 工作树之外。
- 限制对结果的访问，设置保留期限，并在共享或上传前进行审查。
- 保持包、运行时和依赖项为最新版本。

有关 Codex 沙箱、审批和网络控制的详细信息，请参阅 [Agent 审批与安全](https://developers.openai.com/codex/agent-approvals-security/)。有关漏洞标识符和披露时间线的信息，请参阅 [OpenAI 的 CVE 分配政策](https://openai.com/policies/openai-cve-assignment-policy/)。